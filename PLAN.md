# PLAN: 正規表現テスター

## 1. 概要

正規表現パターン・フラグ・テスト対象文字列を入力すると、その場でマッチ箇所を本文中にハイライト表示する静的単一ページアプリを作る。マッチ件数の表示、無効なパターン入力時のエラーメッセージ表示を含む。値は保存せず、入力のたびに即時再評価する使い捨てツールとする。

## 2. 意図（明示）

コードを書きながら正規表現の挙動を本番コードで試す前に、手元でさっと検証したいエンジニアが、値を保存せず使い捨てで確認するための道具。

## 3. 受け入れ条件

- [ ] 正規表現パターンとテスト対象文字列を入力すると、パターンに一致した箇所が本文中でハイライト表示される（`<mark>` 等の判別可能な要素で囲まれる）
- [ ] グローバルフラグ `g` を有効にすると全マッチ件数（例: 「3件一致」）が表示され、無効にすると最初の1件のみがハイライト・件数表示される
- [ ] 無効な正規表現（構文エラー、例: `[a-`）を入力するとエラーメッセージが表示され、ハイライトは行われない
- [ ] パターンまたはテスト文字列を変更すると、ボタン操作なしで結果が即時更新される
- [ ] テスト文字列に `<script>` 等の HTML 特殊文字が含まれても、タグとして解釈されずそのまま文字として表示される
- [ ] マッチが0件のとき「0件一致」等のマッチなしであることが分かる表示になる
- [ ] 入力値は保存されない。パターン・フラグ・テスト文字列を入力した後にページを再読み込みすると、各入力欄は初期状態（パターン・テスト文字列は空、フラグは初期値 `g`）に戻り、入力した値は復元されない

## 4. 実装方針

`public/index.html` 単一ファイル（CSS/JS インライン、バニラJS、ビルドなし）で完結させる。

### レイアウト

- ヘッダー: タイトル
- 入力部: パターン入力（`#pattern`、text input）＋フラグ入力（`#flags`、text input、初期値 `g`）、テスト文字列入力（`#test-input`、textarea）
- 結果部: エラー表示領域（`#error`）、件数表示（`#match-count`）、ハイライト済み本文（`#result`、`white-space: pre-wrap`）
- フッター: AGENTS.md 指定の固定マークアップで apps.jozo.beer への導線。body はセンタリングレイアウトにしない（通常フロー＋`max-width` コンテナ）ため `</body>` 直前でよい
- `<link rel="icon" href="data:image/svg+xml,...">` で正規表現テーマ（例: `.*` の文字）のインライン favicon

### 主要関数

- `escapeHtml(s)`: `& < > " '` を実体参照へ変換
- `collectMatches(regex, text)`: `regex.exec` ループでマッチ範囲 `[{start, end}]` を収集。非グローバル時は1件で打ち切り。空マッチ時は `lastIndex` を手動インクリメントして無限ループを防止
- `render()`: 入力値から `new RegExp(pattern, flags)` を try/catch で生成。失敗時は `#error` にメッセージ表示・件数と結果をクリア。成功時はマッチ範囲で本文を分割し、各断片を `escapeHtml` してから マッチ部を `<mark>` で包んで `#result.innerHTML` へ。件数を `#match-count` に表示
- 3つの入力すべての `input` イベントに `render()` を接続（デバウンス不要な軽量処理）
- 永続化は一切行わない: `localStorage` / `sessionStorage` / Cookie / URL パラメータへの入力値の保存・復元コードを書かない

### テスト

`tests/app.spec.ts` に受け入れ条件7件それぞれの Playwright テストを追記（雛形スモークテストは残す）。`page.fill` で入力し `#result mark` の件数・`#match-count` のテキスト・`#error` の表示を検証する。非保存の条件は、入力後に `page.reload()` を実行し、各入力欄が初期状態に戻っている（入力値が復元されていない）ことを検証する。

### 検証

`npm run verify` と `npm test` が通ること。
